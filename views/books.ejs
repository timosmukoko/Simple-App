<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Manage Books</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>

  <nav>
    <a href="/">Home</a>
    <a href="/books/manage">Manage Books</a>
  </nav>

  <h1>Welcome to TMSU2025 Book </h1>
  <p>Click on "Manage Books" to add, edit, or delete books.</p>

  <!-- Add or Edit Book Form -->
  <h2><%= book.id ? "Edit Book" : "Add a New Book" %></h2>
  <form action="/books" method="POST">
    <!-- Hidden field for book ID if editing -->
    <input type="hidden" name="id" value="<%= book.id ? book.id : '' %>">
    <input type="text" name="title" value="<%= book.title ? book.title : '' %>" placeholder="Title" required>
    <input type="text" name="author" value="<%= book.author ? book.author : '' %>" placeholder="Author" required>
    <button type="submit"><%= book.id ? "Update Book" : "Add Book" %></button>
  </form>

  <h2>Books List</h2>
  <ul>
    <% books.forEach(book => { %>
      <li>
        <%= book.title %> by <%= book.author %>
        <!-- Delete form -->
        <form action="/books/delete/<%= book.id %>" method="POST" style="display:inline;">
          <button type="submit">Delete</button>
        </form>
        <!-- Edit link -->
        <a href="/books/manage?edit=<%= book.id %>">Edit</a>
      </li>
    <% }) %>
  </ul>

</body>
</html>
